[package]
name = "hash_wizard"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
sha2 = "0.10"
rand = "0.8"
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
tempfile = "3.20.0"
regex = "1.11.1"

